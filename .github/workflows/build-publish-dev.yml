name: Build and Publish Dev Docker Image

on:
  push:
    branches:
      - develop

jobs:
  build-and-publish:
    runs-on: ubuntu-latest
    steps:
      - name: Build dev
        run: echo 'Building and deploying to dev'
